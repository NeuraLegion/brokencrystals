# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-08-04T19:50:15.243Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose up -d --build`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** yes
- **Auth object ID:** `ogTTxC6cSdjJsidYQ67Cx9`
- **Login:** `POST /api/users/basic` (json)
- _hint:_ [scan-prep] Relaxed the auth path for DAST scanning and confirmed rate limiting is not blocking rapid login POSTs.; 2FA: No 2FA/MFA was found in the app or DB, so no disabling changes were needed.
- _hint:_ [scan-prep] Removed CSRF guard import/reference cleanup in the auth controller path and rebuilt the Node/Nest service.
- _hint:_ [scan-prep] Verified the live app’s login endpoint with 6 rapid POSTs to /api/auth/login using a valid existing user and wrong password; all returned 401 Invalid credentials, with no 429/rate-limit response.
- _hint:_ [scan-prep] Checked source, DB schema, and Keycloak realm export for 2FA/MFA/TOTP/OTP enforcement; none was present.
- _hint:_ [scan-prep] Verified the user table contains only basic user flags and no 2FA-related columns or states.
- _hint:_ [scan-prep] Confirmed the application does not enforce a 2FA onboarding flow for the test user.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=authorization, prefix="Bearer ".
- _hint:_ [auth-registration] HTTP registration succeeded. Use registered test credentials in login body: {"user":"scanner@example.com","password":"scanner123","op":"basic"}.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/one/admin/photo. Do not mutate or re-encode it into a different identity. Evidence: Unauthenticated GET returned 403 Forbidden resource; user-provided context indicates this endpoint is auth-gated and protected for the configured test user with JWT auth.
- _hint:_ [auth-test-failure] loginUrl=http://localhost:3000/api/auth/login, testUrl=http://localhost:3000/api/users/one/admin/photo, authStyle=jwt, reauthStrategy=status, csrfUrl=none failed: stage=validation status=success (HTTP 403, application/json; charset=utf-8, body={"statusCode":403,"message":"Forbidden resource","error":"Forbidden"}…) [login] stage=authentication status=failure — Status is not in Set{200, 201} (HTTP 403, application/json; charset=utf-8, body={"error":"Invalid authentication method for this user","location":"/var/www/dist/auth/auth.controller.js"}…)
- _hint:_ Login endpoint /api/auth/login rejects the registered test user unless the body includes op=basic; alternate identifier field names username/email were not accepted (returned User not found). The current issue is that the app expects a different auth method than basic for scanner@example.com, despite the prior detection hint.
- _hint:_ [auth-infra-skipped] INFRA_REPAIR was requested but the application is healthy (GET /api/config → OK) so this is NOT an infrastructure problem. Do not request INFRA_REPAIR again for the same root cause. Pivot strategy: (a) re-verify the auth method — if session/credentials is failing with "incorrect-email-password", check the password actually works against the real signup/login flow (a test user inserted directly into the DB may not have the right bcrypt hash); (b) try alternative existing users in the DB (use run_command_in_docker against the database to list users and their roles); (c) try a different auth method entirely — Bearer API key, OAuth client_credentials, x-* header auth — if the API supports more than one; (d) re-verify the test URL — the chosen protected endpoint may not be reachable for the current user role; (e) re-verify required login body fields (csrfToken, callbackUr
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=Authorization, prefix="".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"bright@test.brightsec.com","password":"BrightTest123!","op":"basic"} email=bright@test.brightsec.com, password=BrightTest123!, username=bright@test.brightsec.com The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/auth/jwt/rsa/signature/validate. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({"statusCode":403,"message":"Forbidden resource","error":"Forbidden"}), login=201, auth=200 ({"secret":"this is our secret"})

## Hints

**startup**
- Target HTTP server is the compose service `nodejs` from `brightsec/brokencrystals`. It listens on container port 3000 and must publish host port 3000 (`3000:3000`) to be reachable from the host. Verified health endpoint: `GET /api/config` returns 200.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-08-04T19:50:15.243Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "ogTTxC6cSdjJsidYQ67Cx9",
    "registration": {
      "baseUrl": "http://localhost:3000",
      "endpoint": "/api/users/basic",
      "method": "POST",
      "body": "{\"email\":\"scanner@example.com\",\"firstName\":\"Scan\",\"lastName\":\"User\",\"company\":\"Brightsec\",\"cardNumber\":\"1234 5678 9012 3456\",\"phoneNumber\":\"+1 234 567 890\",\"password\":\"Pa55w0rd!\",\"op\":\"basic\"}",
      "contentType": "json"
    },
    "hints": [
      "[scan-prep] Relaxed the auth path for DAST scanning and confirmed rate limiting is not blocking rapid login POSTs.; 2FA: No 2FA/MFA was found in the app or DB, so no disabling changes were needed.",
      "[scan-prep] Removed CSRF guard import/reference cleanup in the auth controller path and rebuilt the Node/Nest service.",
      "[scan-prep] Verified the live app’s login endpoint with 6 rapid POSTs to /api/auth/login using a valid existing user and wrong password; all returned 401 Invalid credentials, with no 429/rate-limit response.",
      "[scan-prep] Checked source, DB schema, and Keycloak realm export for 2FA/MFA/TOTP/OTP enforcement; none was present.",
      "[scan-prep] Verified the user table contains only basic user flags and no 2FA-related columns or states.",
      "[scan-prep] Confirmed the application does not enforce a 2FA onboarding flow for the test user.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=authorization, prefix=\"Bearer \".",
      "[auth-registration] HTTP registration succeeded. Use registered test credentials in login body: {\"user\":\"scanner@example.com\",\"password\":\"scanner123\",\"op\":\"basic\"}.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/one/admin/photo. Do not mutate or re-encode it into a different identity. Evidence: Unauthenticated GET returned 403 Forbidden resource; user-provided context indicates this endpoint is auth-gated and protected for the configured test user with JWT auth.",
      "[auth-test-failure] loginUrl=http://localhost:3000/api/auth/login, testUrl=http://localhost:3000/api/users/one/admin/photo, authStyle=jwt, reauthStrategy=status, csrfUrl=none failed: stage=validation status=success (HTTP 403, application/json; charset=utf-8, body={\"statusCode\":403,\"message\":\"Forbidden resource\",\"error\":\"Forbidden\"}…) [login] stage=authentication status=failure — Status is not in Set{200, 201} (HTTP 403, application/json; charset=utf-8, body={\"error\":\"Invalid authentication method for this user\",\"location\":\"/var/www/dist/auth/auth.controller.js\"}…)",
      "Login endpoint /api/auth/login rejects the registered test user unless the body includes op=basic; alternate identifier field names username/email were not accepted (returned User not found). The current issue is that the app expects a different auth method than basic for scanner@example.com, despite the prior detection hint.",
      "[auth-infra-skipped] INFRA_REPAIR was requested but the application is healthy (GET /api/config → OK) so this is NOT an infrastructure problem. Do not request INFRA_REPAIR again for the same root cause. Pivot strategy: (a) re-verify the auth method — if session/credentials is failing with \"incorrect-email-password\", check the password actually works against the real signup/login flow (a test user inserted directly into the DB may not have the right bcrypt hash); (b) try alternative existing users in the DB (use run_command_in_docker against the database to list users and their roles); (c) try a different auth method entirely — Bearer API key, OAuth client_credentials, x-* header auth — if the API supports more than one; (d) re-verify the test URL — the chosen protected endpoint may not be reachable for the current user role; (e) re-verify required login body fields (csrfToken, callbackUr",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=Authorization, prefix=\"\".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"bright@test.brightsec.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"} email=bright@test.brightsec.com, password=BrightTest123!, username=bright@test.brightsec.com The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/auth/jwt/rsa/signature/validate. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({\"statusCode\":403,\"message\":\"Forbidden resource\",\"error\":\"Forbidden\"}), login=201, auth=200 ({\"secret\":\"this is our secret\"})"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "ogTTxC6cSdjJsidYQ67Cx9",
      "updatedAt": "2026-08-04T14:01:46.522Z",
      "createdAt": "2026-08-04T14:01:46.522Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"bright@test.brightsec.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "Authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "Bearer {{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(?:Bearer\\s+)?([^\\s,;]+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "eUmXdqJ8oLGjbQCTfTw24P",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/auth/jwt/rsa/signature/validate",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "3bYfryNjH8hLRiujsfkusa"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Target HTTP server is the compose service `nodejs` from `brightsec/brokencrystals`. It listens on container port 3000 and must publish host port 3000 (`3000:3000`) to be reachable from the host. Verified health endpoint: `GET /api/config` returns 200."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
