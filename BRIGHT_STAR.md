# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-28T19:08:14.848Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `bright_test`
  - password: `BrightTest123!`
  - email: `bright@test.com`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `12deWAeCN9PUisAT3c5rqF`
- **Login:** `POST /api/users/basic` (json)
- _hint:_ [scan-prep] Prepared the NestJS app for DAST by disabling CSRF enforcement on the main login endpoint, extending session lifetime, and making Keycloak auth/session timeouts highly permissive; then verified with 5 rapid POST requests to /api/auth/login using the seeded admin/admin account, all of which returned 201 with no throttling or rate-limit errors.
- _hint:_ [scan-prep] Removed the CSRF guard from POST /api/auth/login in src/auth/auth.controller.ts so scanners can submit login requests without obtaining or maintaining CSRF tokens.
- _hint:_ [scan-prep] Extended the Fastify session cookie lifetime in src/main.ts by adding a one-year maxAge, reducing aggressive session expiration during long scans.
- _hint:_ [scan-prep] Relaxed Keycloak token/session timeout settings in keycloak/imports/realm-export.json by raising access token, SSO session, action token, device code, and login code lifespans to 999999.
- _hint:_ [scan-prep] Confirmed the Keycloak realm import already had bruteForceProtected set to false, so account lockout/brute-force protection was not active.
- _hint:_ [scan-prep] Rebuilt and recreated the nodejs and keycloak services with docker compose so the source and realm changes took effect persistently.
- _hint:_ [setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"bright@test.com","password":"BrightTest123!","op":"basic"}. Token location=header, field/header=authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"bright@test.com","password":"BrightTest123!","op":"basic"} email=bright@test.com, password=BrightTest123! The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: Verified with live auth test: unauthenticated GET /api/users/me returned 403 Forbidden; login at POST /api/auth/login returned 201; authenticated GET /api/users/me returned 200 with user JSON.

## Hints

**startup**
- Repo is Broken Crystals: NestJS/Fastify server plus React client. Recommended local startup in README is `docker compose --file=compose.local.yml up -d` and app should publish http://localhost:3000 with health-ish endpoint /api/config.
- App source Dockerfile uses node:18-alpine, builds server and client, and container runs `npm run start:prod`. Main server listens on 0.0.0.0:3000 and gRPC on 0.0.0.0:5000.
- Successful reproducible startup: `docker compose -f compose.local.yml up -d --build`. Host base URL is http://localhost:3000 and `/api/config` returns HTTP 200 once up.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-28T19:08:14.848Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "bright_test",
      "password": "BrightTest123!",
      "email": "bright@test.com"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "12deWAeCN9PUisAT3c5rqF",
    "registration": {
      "baseUrl": "http://localhost:3000",
      "endpoint": "/api/users/basic",
      "method": "POST",
      "body": "{\"email\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"firstName\":\"Bright\",\"lastName\":\"Test\",\"company\":\"Bright\",\"cardNumber\":\"4111111111111111\",\"phoneNumber\":\"1234567890\",\"op\":\"basic\"}",
      "contentType": "json"
    },
    "hints": [
      "[scan-prep] Prepared the NestJS app for DAST by disabling CSRF enforcement on the main login endpoint, extending session lifetime, and making Keycloak auth/session timeouts highly permissive; then verified with 5 rapid POST requests to /api/auth/login using the seeded admin/admin account, all of which returned 201 with no throttling or rate-limit errors.",
      "[scan-prep] Removed the CSRF guard from POST /api/auth/login in src/auth/auth.controller.ts so scanners can submit login requests without obtaining or maintaining CSRF tokens.",
      "[scan-prep] Extended the Fastify session cookie lifetime in src/main.ts by adding a one-year maxAge, reducing aggressive session expiration during long scans.",
      "[scan-prep] Relaxed Keycloak token/session timeout settings in keycloak/imports/realm-export.json by raising access token, SSO session, action token, device code, and login code lifespans to 999999.",
      "[scan-prep] Confirmed the Keycloak realm import already had bruteForceProtected set to false, so account lockout/brute-force protection was not active.",
      "[scan-prep] Rebuilt and recreated the nodejs and keycloak services with docker compose so the source and realm changes took effect persistently.",
      "[setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"} email=bright@test.com, password=BrightTest123! The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: Verified with live auth test: unauthenticated GET /api/users/me returned 403 Forbidden; login at POST /api/auth/login returned 201; authenticated GET /api/users/me returned 200 with user JSON."
    ],
    "authObjectJson": {
      "otp": null,
      "id": "12deWAeCN9PUisAT3c5rqF",
      "updatedAt": "2026-07-28T15:17:55.498Z",
      "createdAt": "2026-07-28T15:17:55.498Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "Authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "Bearer {{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(?:Bearer\\s+)?([^\\s,;]+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "fMdXpuND4Fqv7GT5Juws8p",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/me",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "aAmNiSi9dj7QFo23Y5BBH8"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Repo is Broken Crystals: NestJS/Fastify server plus React client. Recommended local startup in README is `docker compose --file=compose.local.yml up -d` and app should publish http://localhost:3000 with health-ish endpoint /api/config.",
      "App source Dockerfile uses node:18-alpine, builds server and client, and container runs `npm run start:prod`. Main server listens on 0.0.0.0:3000 and gRPC on 0.0.0.0:5000.",
      "Successful reproducible startup: `docker compose -f compose.local.yml up -d --build`. Host base URL is http://localhost:3000 and `/api/config` returns HTTP 200 once up."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
