# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-15T12:26:47.133Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose --file compose.local.yml up -d --build`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `bright_test`
  - password: `BrightTest123!`
  - email: `bright@test.com`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `tE2JDcCVFc4q6JXPcWYpjk`
- _hint:_ [scan-prep] Disabled CSRF enforcement on the login flow and confirmed repeated login POSTs are not rate-limited.; 2FA: No 2FA/MFA enforcement was found, so no changes were required.
- _hint:_ [scan-prep] Relaxed the CSRF guard to always allow login requests for DAST scanning.
- _hint:_ [scan-prep] Verified the auth login endpoint handles rapid repeated POSTs without rate-limit responses (5 rapid POST /api/auth/login requests returned 401 Invalid credentials, not 429/CSRF blocks).
- _hint:_ [scan-prep] Searched the codebase and database schema for 2FA/MFA/TOTP/OTP/verification-related enforcement and found no application-level 2FA implementation.
- _hint:_ [scan-prep] Checked the user schema and seed data; there are no two-factor fields or status flags to disable, and no per-user 2FA activation to clear.
- _hint:_ [scan-prep] Verified the app responds normally at /api/config, indicating the test user is not blocked by a 2FA onboarding gate in this application.
- _hint:_ [setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"bright@test.com","password":"BrightTest123!","op":"basic"}. Token location=header, field/header=authorization, request header=authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"bright@test.com","password":"BrightTest123!","op":"basic"} email=bright@test.com, password=BrightTest123!, username=bright_test The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/one/bright@test.com/info. Do not mutate or re-encode it into a different identity. Evidence: Unauthenticated GET returned 403 Forbidden: {"message":"Forbidden resource","error":"Forbidden","statusCode":403}. Login to POST /api/auth/login with bright@test.com / BrightTest123! succeeds (201) and the app indicates JWT auth is configured for this endpoint family.

## Hints

**startup**
- Use `docker compose --file compose.local.yml up -d --build` to start the app stack. The repo's Dockerfile builds on node:18-alpine, the app serves on container port 3000, and compose.local.yml publishes host port 3000 for the nodejs service via the main app image.
- The running stack serves successfully at baseUrl http://127.0.0.1:3000. Verified readiness endpoint: GET /api/config returns 200 OK once the nodejs container finishes starting.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-15T12:26:47.133Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose --file compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "bright_test",
      "password": "BrightTest123!",
      "email": "bright@test.com"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "tE2JDcCVFc4q6JXPcWYpjk",
    "hints": [
      "[scan-prep] Disabled CSRF enforcement on the login flow and confirmed repeated login POSTs are not rate-limited.; 2FA: No 2FA/MFA enforcement was found, so no changes were required.",
      "[scan-prep] Relaxed the CSRF guard to always allow login requests for DAST scanning.",
      "[scan-prep] Verified the auth login endpoint handles rapid repeated POSTs without rate-limit responses (5 rapid POST /api/auth/login requests returned 401 Invalid credentials, not 429/CSRF blocks).",
      "[scan-prep] Searched the codebase and database schema for 2FA/MFA/TOTP/OTP/verification-related enforcement and found no application-level 2FA implementation.",
      "[scan-prep] Checked the user schema and seed data; there are no two-factor fields or status flags to disable, and no per-user 2FA activation to clear.",
      "[scan-prep] Verified the app responds normally at /api/config, indicating the test user is not blocked by a 2FA onboarding gate in this application.",
      "[setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"} email=bright@test.com, password=BrightTest123!, username=bright_test The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/one/bright@test.com/info. Do not mutate or re-encode it into a different identity. Evidence: Unauthenticated GET returned 403 Forbidden: {\"message\":\"Forbidden resource\",\"error\":\"Forbidden\",\"statusCode\":403}. Login to POST /api/auth/login with bright@test.com / BrightTest123! succeeds (201) and the app indicates JWT auth is configured for this endpoint family."
    ],
    "authObjectJson": {
      "otp": null,
      "id": "tE2JDcCVFc4q6JXPcWYpjk",
      "updatedAt": "2026-07-15T11:54:55.902Z",
      "createdAt": "2026-07-15T11:54:55.902Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  },
                  {
                    "name": "Accept",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "Bearer {{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(?:Bearer\\s+)?([^\\s,;]+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "hQg3feiBBdERvixzC7wwKH",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/one/bright@test.com/info",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "ppXruMQ2CL1uzdP5tvzs8H"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Use `docker compose --file compose.local.yml up -d --build` to start the app stack. The repo's Dockerfile builds on node:18-alpine, the app serves on container port 3000, and compose.local.yml publishes host port 3000 for the nodejs service via the main app image.",
      "The running stack serves successfully at baseUrl http://127.0.0.1:3000. Verified readiness endpoint: GET /api/config returns 200 OK once the nodejs container finishes starting."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
