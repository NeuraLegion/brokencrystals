# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-08-04T19:57:47.195Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `admin`
  - password: `admin`
  - email: `admin`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `mi4Sh1VgMyJA7YzAchGfoj`
- _hint:_ [scan-prep] No rate limits or throttles were found; the app already allows rapid auth attempts without blocking, and this was verified against the live login endpoint.; 2FA: The app itself does not enforce 2FA; 2FA-related settings live in Keycloak realm import, and no test-user 2FA block was found in the application code or database schema.
- _hint:_ [scan-prep] Inspected the NestJS/Fastify app and confirmed there is no rate-limiting/throttling middleware, guard, or login lockout logic in the source code.
- _hint:_ [scan-prep] Queried the running container’s environment and database-backed runtime state for rate/limit/throttle-related settings; none were present.
- _hint:_ [scan-prep] Verified the login path directly with 5 rapid POST requests to /api/auth/login using an existing account; all returned 401 Invalid credentials with no 429/throttle response.
- _hint:_ [scan-prep] No application-level 2FA/MFA enforcement found in the NestJS app code or DB schema.
- _hint:_ [scan-prep] Detected Keycloak realm import containing OTP/TOTP configuration and auth flows, but no user-specific 2FA enrollment blocking in the app itself.
- _hint:_ [scan-prep] Updated compose.yml to start Keycloak with theme cache disabled so realm changes are easier to apply on restart.
- _hint:_ [setup-credentials] First-run setup created user username=admin, email=admin, password=admin. Prefer these credentials for auth.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/auth/jwt/rsa/signature/validate. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({"message":"Forbidden resource","error":"Forbidden","statusCode":403}), login=201, auth=200 ({"secret":"this is our secret"})

## Rate Limits / Scan Prep

- [brokencrystals-db-1] `sh -lc "psql -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\" -c \"select \"email\" from \"user\" where is_basic=true limit 5;\""`

## Hints

**startup**
- Target server is the NestJS/Fastify app in src/main.ts. It listens on 0.0.0.0:3000 and also starts a gRPC microservice on 0.0.0.0:5000. Compose.local.yml already builds from the repo Dockerfile and publishes 3000, 5000, 8080, 8081, 1080, 1025. The app expects URL=http://localhost:3000 and optionally GRPC_WEB_PROXY_URL, CHAT_API_URL, CHAT_API_MODEL, CHAT_API_TOKEN, CHAT_API_MAX_TOKENS.
- compose.local.yml successfully boots the app stack with `docker compose -f compose.local.yml up -d --build`. The app responds on host port 3000; a verified health endpoint is GET /api/config returning 200. The nodejs container reaches healthy state after startup, while dependencies db/keycloak/keycloak-db/mailcatcher/ollama are healthy.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-08-04T19:57:47.195Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "admin",
      "password": "admin",
      "email": "admin"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "mi4Sh1VgMyJA7YzAchGfoj",
    "hints": [
      "[scan-prep] No rate limits or throttles were found; the app already allows rapid auth attempts without blocking, and this was verified against the live login endpoint.; 2FA: The app itself does not enforce 2FA; 2FA-related settings live in Keycloak realm import, and no test-user 2FA block was found in the application code or database schema.",
      "[scan-prep] Inspected the NestJS/Fastify app and confirmed there is no rate-limiting/throttling middleware, guard, or login lockout logic in the source code.",
      "[scan-prep] Queried the running container’s environment and database-backed runtime state for rate/limit/throttle-related settings; none were present.",
      "[scan-prep] Verified the login path directly with 5 rapid POST requests to /api/auth/login using an existing account; all returned 401 Invalid credentials with no 429/throttle response.",
      "[scan-prep] No application-level 2FA/MFA enforcement found in the NestJS app code or DB schema.",
      "[scan-prep] Detected Keycloak realm import containing OTP/TOTP configuration and auth flows, but no user-specific 2FA enrollment blocking in the app itself.",
      "[scan-prep] Updated compose.yml to start Keycloak with theme cache disabled so realm changes are easier to apply on restart.",
      "[setup-credentials] First-run setup created user username=admin, email=admin, password=admin. Prefer these credentials for auth.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/auth/jwt/rsa/signature/validate. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({\"message\":\"Forbidden resource\",\"error\":\"Forbidden\",\"statusCode\":403}), login=201, auth=200 ({\"secret\":\"this is our secret\"})"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "mi4Sh1VgMyJA7YzAchGfoj",
      "updatedAt": "2026-08-04T14:09:49.165Z",
      "createdAt": "2026-08-04T14:09:49.165Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  },
                  {
                    "name": "Accept",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "Authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "Bearer {{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(?:Bearer\\s+)?([^\\s,;]+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "eUmXdqJ8oLGjbQCTfTw24P",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/auth/jwt/rsa/signature/validate",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "64fdynjF22Ecgi8yB4RqGD"
      },
      "staticHeaders": null
    }
  },
  "limits": {
    "scanPrepReplayCommands": [
      {
        "container": "brokencrystals-db-1",
        "command": "sh -lc \"psql -U \\\"$POSTGRES_USER\\\" -d \\\"$POSTGRES_DB\\\" -c \\\"select \\\"email\\\" from \\\"user\\\" where is_basic=true limit 5;\\\"\""
      }
    ]
  },
  "hints": {
    "startup": [
      "Target server is the NestJS/Fastify app in src/main.ts. It listens on 0.0.0.0:3000 and also starts a gRPC microservice on 0.0.0.0:5000. Compose.local.yml already builds from the repo Dockerfile and publishes 3000, 5000, 8080, 8081, 1080, 1025. The app expects URL=http://localhost:3000 and optionally GRPC_WEB_PROXY_URL, CHAT_API_URL, CHAT_API_MODEL, CHAT_API_TOKEN, CHAT_API_MAX_TOKENS.",
      "compose.local.yml successfully boots the app stack with `docker compose -f compose.local.yml up -d --build`. The app responds on host port 3000; a verified health endpoint is GET /api/config returning 200. The nodejs container reaches healthy state after startup, while dependencies db/keycloak/keycloak-db/mailcatcher/ollama are healthy."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
