# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-10T07:23:10.460Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build (after patching Dockerfile base images from node:18-alpine to node:18-bookworm/node:18-bookworm-slim)`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `bright_test`
  - password: `BrightTest123!`
  - email: `bright@test.com`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `aL9Y66fzxNgPzAPeLWqkfr`
- _hint:_ [scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change
- _hint:_ [setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"bright@test.com","password":"BrightTest123!","op":"basic"}. Token location=header, field/header=authorization (response header, RS256-signed JWT string), request header=authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"bright@test.com","password":"BrightTest123!","op":"basic"} email=bright@test.com, password=BrightTest123!, username=bright_test The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.

## Hints

**startup**
- Startup command: docker compose -f compose.local.yml up -d --build (after patching Dockerfile base images to node:18-bookworm / node:18-bookworm-slim). Verify with curl http://localhost:3000/api/config -> 200 JSON. Teardown: docker compose -f compose.local.yml down.

**infra**
- BrokenCrystals app (NestJS+Fastify) Dockerfile used node:18-alpine base, but libxmljs native binding fails on Alpine on arm64 hosts (ERR_DLOPEN_FAILED / missing ld-linux-aarch64.so.1, then GLIBC_2.33 not found on bullseye). Fix: change Dockerfile base images from node:18-alpine to node:18-bookworm (build stage) and node:18-bookworm-slim (production stage) so libxmljs native binary loads correctly. Then use compose.local.yml (docker compose -f compose.local.yml up -d --build) which builds nodejs from source and wires db, keycloak, mailcatcher, ollama, grpcwebproxy. App listens on port 3000, container healthcheck hits GET /api/config which returns 200 with JSON when healthy.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-10T07:23:10.460Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build (after patching Dockerfile base images from node:18-alpine to node:18-bookworm/node:18-bookworm-slim)",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "bright_test",
      "password": "BrightTest123!",
      "email": "bright@test.com"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "aL9Y66fzxNgPzAPeLWqkfr",
    "hints": [
      "[scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change",
      "[setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}. Token location=header, field/header=authorization (response header, RS256-signed JWT string), request header=authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"bright@test.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"} email=bright@test.com, password=BrightTest123!, username=bright_test The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON."
    ]
  },
  "hints": {
    "startup": [
      "Startup command: docker compose -f compose.local.yml up -d --build (after patching Dockerfile base images to node:18-bookworm / node:18-bookworm-slim). Verify with curl http://localhost:3000/api/config -> 200 JSON. Teardown: docker compose -f compose.local.yml down."
    ],
    "infra": [
      "BrokenCrystals app (NestJS+Fastify) Dockerfile used node:18-alpine base, but libxmljs native binding fails on Alpine on arm64 hosts (ERR_DLOPEN_FAILED / missing ld-linux-aarch64.so.1, then GLIBC_2.33 not found on bullseye). Fix: change Dockerfile base images from node:18-alpine to node:18-bookworm (build stage) and node:18-bookworm-slim (production stage) so libxmljs native binary loads correctly. Then use compose.local.yml (docker compose -f compose.local.yml up -d --build) which builds nodejs from source and wires db, keycloak, mailcatcher, ollama, grpcwebproxy. App listens on port 3000, container healthcheck hits GET /api/config which returns 200 with JSON when healthy."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
