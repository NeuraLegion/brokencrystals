# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-08-05T13:17:52.188Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `admin`
  - password: `admin`
  - email: `admin`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `n6NkzYDZ1zoenZWizwt179`
- _hint:_ [scan-prep] Patched and rebuilt the NestJS app, verified the app is reachable again, and confirmed rapid repeated POSTs to both the app login endpoint and Keycloak token endpoint return normal auth failures (401) rather than any rate-limit, throttle, lockout, or CSRF-block responses.
- _hint:_ [scan-prep] Removed the code-level CSRF guard from the real auth POST endpoint (`POST /api/auth/login`) so scanners can submit repeated login requests without token/cookie enforcement.
- _hint:_ [scan-prep] Confirmed Keycloak realm runtime/import settings are permissive for login protections: `bruteForceProtected=false` and all brute-force wait/failure thresholds are set to `999999` in `keycloak/imports/realm-export.json`.
- _hint:_ [scan-prep] Patched production bootstrap to support `DISABLE_CLUSTER=true` and set that in `compose.local.yml` so the rebuilt NestJS service starts as a single process for reliable restart/verification after source changes.
- _hint:_ [scan-prep] Updated `.dockerignore` to exclude `.data` so the targeted Docker rebuild could complete successfully without permission errors.
- _hint:_ [setup-credentials] First-run setup created user username=admin, email=admin, password=admin. Prefer these credentials for auth.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({"message":"Forbidden resource","error":"Forbidden","statusCode":403}), login=201, auth=200 ({"firstName":"admin","lastName":"admin","id":1,"isAdmin":true})

## Rate Limits / Scan Prep

- [brokencrystals-nodejs-1] `node -e "const argon2=require('argon2'); Promise.all([argon2.verify('$argon2id$v=19$m=65536,t=3,p=4$jmtTCTEcjngErif00RfYAg$biS59Ixnrz+dHeJrJ91ybmHt+4wrVgcH3RXvfaqZtNI','admin'),argon2.verify('$argon2id$v=19$m=65536,t=3,p=4$hJX1v2kH3UFlEOhZFZn3RQ$oXDFhwgoxosiunmy720fBEBGiin0XNeTvDlDk3dUAT4','user')]).then(r=>console.log(JSON.stringify(r))).catch(e=>{console.error(e);process.exit(1)})"`
- [brokencrystals-keycloak-db-1] `psql -U keycloak -d keycloak -c "select username, enabled from user_entity;"`
- [brokencrystals-nodejs-1] `node - <<'NODE'
const argon2=require('argon2');
(async()=>{
const a='$argon2id$v=19$m=65536,t=3,p=4$jmtTCTEcjngErif00RfYAg$biS59Ixnrz+dHeJrJ91ybmHt+4wrVgcH3RXvfaqZtNI';
const u='$argon2id$v=19$m=65536,t=3,p=4$hJX1v2kH3UFlEOhZFZn3RQ$oXDFhwgoxosiunmy720fBEBGiin0XNeTvDlDk3dUAT4';
console.log('admin', await argon2.verify(a,'admin'));
console.log('user', await argon2.verify(u,'user'));
})().catch(e=>{console.error(e);process.exit(1)})
NODE`

## Hints

**startup**
- Use `docker compose -f compose.local.yml up -d --build` from repo root. The app server is the `nodejs` service, publishes host port 3000, binds to 0.0.0.0:3000, and `/api/config` returns 200 when ready.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-08-05T13:17:52.188Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "admin",
      "password": "admin",
      "email": "admin"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "n6NkzYDZ1zoenZWizwt179",
    "hints": [
      "[scan-prep] Patched and rebuilt the NestJS app, verified the app is reachable again, and confirmed rapid repeated POSTs to both the app login endpoint and Keycloak token endpoint return normal auth failures (401) rather than any rate-limit, throttle, lockout, or CSRF-block responses.",
      "[scan-prep] Removed the code-level CSRF guard from the real auth POST endpoint (`POST /api/auth/login`) so scanners can submit repeated login requests without token/cookie enforcement.",
      "[scan-prep] Confirmed Keycloak realm runtime/import settings are permissive for login protections: `bruteForceProtected=false` and all brute-force wait/failure thresholds are set to `999999` in `keycloak/imports/realm-export.json`.",
      "[scan-prep] Patched production bootstrap to support `DISABLE_CLUSTER=true` and set that in `compose.local.yml` so the rebuilt NestJS service starts as a single process for reliable restart/verification after source changes.",
      "[scan-prep] Updated `.dockerignore` to exclude `.data` so the targeted Docker rebuild could complete successfully without permission errors.",
      "[setup-credentials] First-run setup created user username=admin, email=admin, password=admin. Prefer these credentials for auth.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({\"message\":\"Forbidden resource\",\"error\":\"Forbidden\",\"statusCode\":403}), login=201, auth=200 ({\"firstName\":\"admin\",\"lastName\":\"admin\",\"id\":1,\"isAdmin\":true})"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "n6NkzYDZ1zoenZWizwt179",
      "updatedAt": "2026-08-05T09:26:26.625Z",
      "createdAt": "2026-08-05T09:26:26.625Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  },
                  {
                    "name": "Accept",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "Authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "Bearer {{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(?:Bearer\\s+)?([^\\s,;]+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "eUmXdqJ8oLGjbQCTfTw24P",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/me",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "cmPJLPdXhfy3PRvSKqPBHe"
      },
      "staticHeaders": null
    }
  },
  "limits": {
    "scanPrepReplayCommands": [
      {
        "container": "brokencrystals-nodejs-1",
        "command": "node -e \"const argon2=require('argon2'); Promise.all([argon2.verify('$argon2id$v=19$m=65536,t=3,p=4$jmtTCTEcjngErif00RfYAg$biS59Ixnrz+dHeJrJ91ybmHt+4wrVgcH3RXvfaqZtNI','admin'),argon2.verify('$argon2id$v=19$m=65536,t=3,p=4$hJX1v2kH3UFlEOhZFZn3RQ$oXDFhwgoxosiunmy720fBEBGiin0XNeTvDlDk3dUAT4','user')]).then(r=>console.log(JSON.stringify(r))).catch(e=>{console.error(e);process.exit(1)})\""
      },
      {
        "container": "brokencrystals-keycloak-db-1",
        "command": "psql -U keycloak -d keycloak -c \"select username, enabled from user_entity;\""
      },
      {
        "container": "brokencrystals-nodejs-1",
        "command": "node - <<'NODE'\nconst argon2=require('argon2');\n(async()=>{\nconst a='$argon2id$v=19$m=65536,t=3,p=4$jmtTCTEcjngErif00RfYAg$biS59Ixnrz+dHeJrJ91ybmHt+4wrVgcH3RXvfaqZtNI';\nconst u='$argon2id$v=19$m=65536,t=3,p=4$hJX1v2kH3UFlEOhZFZn3RQ$oXDFhwgoxosiunmy720fBEBGiin0XNeTvDlDk3dUAT4';\nconsole.log('admin', await argon2.verify(a,'admin'));\nconsole.log('user', await argon2.verify(u,'user'));\n})().catch(e=>{console.error(e);process.exit(1)})\nNODE"
      }
    ]
  },
  "hints": {
    "startup": [
      "Use `docker compose -f compose.local.yml up -d --build` from repo root. The app server is the `nodejs` service, publishes host port 3000, binds to 0.0.0.0:3000, and `/api/config` returns 200 when ready."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
