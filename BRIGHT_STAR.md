# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-08T14:44:57.864Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `bright_test`
  - password: `BrightTest123!`
  - email: `bright@test.com`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `47EbrCCKkRRNgqr86qnzRW`
- **Login:** `POST /api/users/basic` (json)
- _hint:_ [scan-prep] Verified the real auth endpoint accepts 5 rapid POST login attempts without throttling or lockout, and found no active scanner-blocking rate-limit/security control requiring changes.
- _hint:_ [scan-prep] Inspected NestJS/Fastify application source, auth controller, guards, middleware, and compose files for rate limiting, lockout, CAPTCHA, CSRF enforcement, session timeout, and request-throttling controls.
- _hint:_ [scan-prep] Queried the live app behavior via the real login processing endpoint POST /api/auth/login using seeded credentials from pg.sql (admin/admin) and invalid-password probes on the same existing account.
- _hint:_ [scan-prep] Confirmed there is no active framework-level or code-level login rate limiting/account lockout blocker in the running app: 5 rapid POST login probes to /api/auth/login all returned 401 Invalid credentials, with no 429 or throttle-related response body.
- _hint:_ [scan-prep] Confirmed no CAPTCHA or bot challenge exists on auth flow, and no active CSRF guard is applied to the login route in the current codebase.
- _hint:_ [scan-prep] No code or config changes were required because the app was already permissive enough for DAST on the login/auth path.
- _hint:_ [setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"authtest@example.com","password":"BrightTest123!","op":"basic"}. Token location=header, field/header=authorization, request header=authorization, prefix="".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: Verified with Bright auth check: unauthenticated GET returned 403 Forbidden; after POST /api/auth/login with admin/admin, authenticated GET returned 200 with user profile data.

## Hints

**startup**
- Repo is Broken Crystals (NestJS + React) and README recommends starting local dev with `docker compose --file=compose.local.yml up -d`; app should be reachable on host port 3000 with health-like endpoint `/api/config`.
- Existing Dockerfile uses node:18-bookworm-slim multi-stage build and starts app with `npm run start:prod`; compose.local.yml includes dependencies db, keycloak, mailcatcher, ollama and publishes 3000:3000.
- `docker compose -f compose.local.yml up -d --build` succeeds on this checkout and serves on `http://127.0.0.1:3000`; verified 200 OK from `/api/config`.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-08T14:44:57.864Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "bright_test",
      "password": "BrightTest123!",
      "email": "bright@test.com"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "47EbrCCKkRRNgqr86qnzRW",
    "registration": {
      "baseUrl": "http://localhost:3000",
      "endpoint": "/api/users/basic",
      "method": "POST",
      "body": "{\"email\":\"authtest@example.com\",\"firstName\":\"Auth\",\"lastName\":\"Test\",\"company\":\"TestCo\",\"cardNumber\":\"4111111111111111\",\"phoneNumber\":\"1234567890\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}",
      "contentType": "json"
    },
    "hints": [
      "[scan-prep] Verified the real auth endpoint accepts 5 rapid POST login attempts without throttling or lockout, and found no active scanner-blocking rate-limit/security control requiring changes.",
      "[scan-prep] Inspected NestJS/Fastify application source, auth controller, guards, middleware, and compose files for rate limiting, lockout, CAPTCHA, CSRF enforcement, session timeout, and request-throttling controls.",
      "[scan-prep] Queried the live app behavior via the real login processing endpoint POST /api/auth/login using seeded credentials from pg.sql (admin/admin) and invalid-password probes on the same existing account.",
      "[scan-prep] Confirmed there is no active framework-level or code-level login rate limiting/account lockout blocker in the running app: 5 rapid POST login probes to /api/auth/login all returned 401 Invalid credentials, with no 429 or throttle-related response body.",
      "[scan-prep] Confirmed no CAPTCHA or bot challenge exists on auth flow, and no active CSRF guard is applied to the login route in the current codebase.",
      "[scan-prep] No code or config changes were required because the app was already permissive enough for DAST on the login/auth path.",
      "[setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"authtest@example.com\",\"password\":\"BrightTest123!\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=authorization, prefix=\"\".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: Verified with Bright auth check: unauthenticated GET returned 403 Forbidden; after POST /api/auth/login with admin/admin, authenticated GET returned 200 with user profile data."
    ],
    "authObjectJson": {
      "otp": null,
      "id": "47EbrCCKkRRNgqr86qnzRW",
      "updatedAt": "2026-07-08T14:13:29.704Z",
      "createdAt": "2026-07-08T14:13:29.704Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "{{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(.+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "ckVBa7J9gjoEudrwZy3nHJ",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/me",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "pEr5abnwgqXgShFTcJSMqp"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Repo is Broken Crystals (NestJS + React) and README recommends starting local dev with `docker compose --file=compose.local.yml up -d`; app should be reachable on host port 3000 with health-like endpoint `/api/config`.",
      "Existing Dockerfile uses node:18-bookworm-slim multi-stage build and starts app with `npm run start:prod`; compose.local.yml includes dependencies db, keycloak, mailcatcher, ollama and publishes 3000:3000.",
      "`docker compose -f compose.local.yml up -d --build` succeeds on this checkout and serves on `http://127.0.0.1:3000`; verified 200 OK from `/api/config`."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
