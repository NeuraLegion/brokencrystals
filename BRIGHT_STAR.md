# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-08-05T10:59:48.412Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `admin`
  - password: `admin`
  - email: `admin`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `iCnHDaeNNrFMKqtdyHuz3a`
- **Login:** `POST /api/users/basic` (json)
- _hint:_ [scan-prep] Verified the real login POST path accepts 5 rapid attempts without rate limiting or other blocking controls.
- _hint:_ [scan-prep] Confirmed the app’s auth login POST endpoint is not rate-limited by issuing 5 rapid POST /api/auth/login probes with the same invalid credentials; all returned HTTP 401 with the expected Invalid credentials body, not 429/throttle responses.
- _hint:_ [scan-prep] Inspected the NestJS auth/csrf flow and runtime/container settings; no rate-limit, throttle, CAPTCHA, or lockout mechanisms were present in the codebase or container env for this app.
- _hint:_ [scan-prep] Verified the running database contains the seeded users needed for auth-path testing (admin, user) and used the real login processing endpoint rather than the login page.
- _hint:_ [setup-credentials] First-run setup created user username=admin, email=admin, password=admin. Prefer these credentials for auth.
- _hint:_ [auth-detection] session auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({"message":"Forbidden resource","error":"Forbidden","statusCode":403}), login=201, auth=200 ({"firstName":"admin","lastName":"admin","id":1,"isAdmin":true})
- _hint:_ [auth-test-failure] raw multistep, testUrl=http://localhost:3000/api/users/me failed: stage=validation status=success (HTTP 403, application/json; charset=utf-8, body={"message":"Forbidden resource","error":"Forbidden","statusCode":403}…) [login] stage=authentication status=success (HTTP 201, application/json; charset=utf-8, body={"email":"admin","ldapProfileLink":"(&(objectClass=person)(objectClass=user)(email=admin))"}…) stage=authorization status=failure — Could not match regex /"accessToken"\s*:\s*"([^"]+)"/ with group '1'

## Hints

**startup**
- Use `docker compose -f compose.local.yml up -d --build` from repo root. The app service is `nodejs`, published on host port 3000 and exposes `http://localhost:3000/api/config` as a working health endpoint. Supporting services are db, keycloak-db, keycloak, mailcatcher, ollama, and grpcwebproxy.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-08-05T10:59:48.412Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "admin",
      "password": "admin",
      "email": "admin"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "iCnHDaeNNrFMKqtdyHuz3a",
    "registration": {
      "baseUrl": "http://localhost:3000",
      "endpoint": "/api/users/basic",
      "method": "POST",
      "body": "{\"email\":\"admin\",\"password\":\"admin\",\"firstName\":\"admin\",\"lastName\":\"admin\",\"op\":\"basic\"}",
      "contentType": "json"
    },
    "hints": [
      "[scan-prep] Verified the real login POST path accepts 5 rapid attempts without rate limiting or other blocking controls.",
      "[scan-prep] Confirmed the app’s auth login POST endpoint is not rate-limited by issuing 5 rapid POST /api/auth/login probes with the same invalid credentials; all returned HTTP 401 with the expected Invalid credentials body, not 429/throttle responses.",
      "[scan-prep] Inspected the NestJS auth/csrf flow and runtime/container settings; no rate-limit, throttle, CAPTCHA, or lockout mechanisms were present in the codebase or container env for this app.",
      "[scan-prep] Verified the running database contains the seeded users needed for auth-path testing (admin, user) and used the real login processing endpoint rather than the login page.",
      "[setup-credentials] First-run setup created user username=admin, email=admin, password=admin. Prefer these credentials for auth.",
      "[auth-detection] session auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin, username=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({\"message\":\"Forbidden resource\",\"error\":\"Forbidden\",\"statusCode\":403}), login=201, auth=200 ({\"firstName\":\"admin\",\"lastName\":\"admin\",\"id\":1,\"isAdmin\":true})",
      "[auth-test-failure] raw multistep, testUrl=http://localhost:3000/api/users/me failed: stage=validation status=success (HTTP 403, application/json; charset=utf-8, body={\"message\":\"Forbidden resource\",\"error\":\"Forbidden\",\"statusCode\":403}…) [login] stage=authentication status=success (HTTP 201, application/json; charset=utf-8, body={\"email\":\"admin\",\"ldapProfileLink\":\"(&(objectClass=person)(objectClass=user)(email=admin))\"}…) stage=authorization status=failure — Could not match regex /\"accessToken\"\\s*:\\s*\"([^\"]+)\"/ with group '1'"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "iCnHDaeNNrFMKqtdyHuz3a",
      "updatedAt": "2026-08-05T10:23:28.879Z",
      "createdAt": "2026-08-05T10:23:28.879Z",
      "repeaterRequired": true,
      "name": "Engine Auth — raw multistep",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}",
                "bodyType": "clear_text",
                "followRedirects": false,
                "maxRedirects": 0
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "authorization",
              "mergeStrategy": "replace",
              "template": "{{ auth_object.stages.login.response.headers | get: '/authorization' }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "eUmXdqJ8oLGjbQCTfTw24P",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/me",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text",
          "followRedirects": false,
          "changeMethodOnRedirect": false,
          "maxRedirects": 0
        },
        "repeaterId": "qvgf1kfZMxQGcgK7jfnwjt"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Use `docker compose -f compose.local.yml up -d --build` from repo root. The app service is `nodejs`, published on host port 3000 and exposes `http://localhost:3000/api/config` as a working health endpoint. Supporting services are db, keycloak-db, keycloak, mailcatcher, ollama, and grpcwebproxy."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
