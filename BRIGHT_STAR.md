# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-08T15:54:31.330Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build`
- **Port:** 3000

## First-Run Setup

- **Completed:** yes
- **Seeded credentials:**
  - username: `bright_test`
  - password: `BrightTest123!`
  - email: `bright@test.com`

## Authentication

- **Has auth:** yes
- **Auth object ID:** `eZWAPbLg2SCTHo4Jt3CQSd`
- **Login:** `POST /api/users/basic` (json)
- _hint:_ [scan-prep] Disabled CSRF enforcement, fixed login POST crashes, stabilized runtime, rebuilt the app, and verified rapid login POSTs are scanner-ready with repeated 401s instead of 5xx/429.
- _hint:_ [scan-prep] Patched src/auth/auth.controller.ts so failed basic logins no longer crash with HTTP 500 when a username is missing; all bad login attempts now consistently return HTTP 401 Invalid credentials.
- _hint:_ [scan-prep] Patched src/auth/csrf.guard.ts to fully bypass CSRF enforcement so scanners can submit auth/form requests without token validation failures.
- _hint:_ [scan-prep] Patched src/main.ts to allow disabling NestJS production clustering via DISABLE_CLUSTER, avoiding multi-worker startup instability during scan prep.
- _hint:_ [scan-prep] Patched compose.yml to set DISABLE_CLUSTER='true' for the nodejs service so the app runs as a single stable worker for automated scanning.
- _hint:_ [scan-prep] Patched .dockerignore to exclude .data so the nodejs image can be rebuilt successfully without Docker build-context permission failures.
- _hint:_ [scan-prep] Rebuilt/restarted the nodejs service and verified 6 rapid POST requests to /api/auth/login all returned HTTP 401 with body {"error":"Invalid credentials"}, with no 429/throttle responses and no 5xx crashes.
- _hint:_ [setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=Authorization, prefix="".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({"message":"Forbidden resource","error":"Forbidden","statusCode":403}), login=201, auth=200 ({"firstName":"admin","lastName":"admin","id":1,"isAdmin":true})

## Rate Limits / Scan Prep

- [brokencrystals-nodejs-1] `printenv | sort | egrep 'DB|POSTGRES|DATABASE|NODE_ENV|URL|JWT|KEYCLOAK|GRPC|FASTIFY|DISABLE|CSRF|SESSION' || true`

## Hints

**startup**
- Repo runs with `docker compose -f compose.local.yml up -d --build`; app is the `nodejs` service built from the repo Dockerfile and published on host port 3000, serving HTTP 200 on `/`. Node server listens on `0.0.0.0:3000`.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-08T15:54:31.330Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build",
    "port": 3000,
    "prerequisites": [],
    "envVars": {}
  },
  "setup": {
    "completed": true,
    "credentials": {
      "username": "bright_test",
      "password": "BrightTest123!",
      "email": "bright@test.com"
    }
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "eZWAPbLg2SCTHo4Jt3CQSd",
    "registration": {
      "baseUrl": "http://localhost:3000",
      "endpoint": "/api/users/basic",
      "method": "POST",
      "body": "email=test@test.com&password=pass",
      "contentType": "json"
    },
    "hints": [
      "[scan-prep] Disabled CSRF enforcement, fixed login POST crashes, stabilized runtime, rebuilt the app, and verified rapid login POSTs are scanner-ready with repeated 401s instead of 5xx/429.",
      "[scan-prep] Patched src/auth/auth.controller.ts so failed basic logins no longer crash with HTTP 500 when a username is missing; all bad login attempts now consistently return HTTP 401 Invalid credentials.",
      "[scan-prep] Patched src/auth/csrf.guard.ts to fully bypass CSRF enforcement so scanners can submit auth/form requests without token validation failures.",
      "[scan-prep] Patched src/main.ts to allow disabling NestJS production clustering via DISABLE_CLUSTER, avoiding multi-worker startup instability during scan prep.",
      "[scan-prep] Patched compose.yml to set DISABLE_CLUSTER='true' for the nodejs service so the app runs as a single stable worker for automated scanning.",
      "[scan-prep] Patched .dockerignore to exclude .data so the nodejs image can be rebuilt successfully without Docker build-context permission failures.",
      "[scan-prep] Rebuilt/restarted the nodejs service and verified 6 rapid POST requests to /api/auth/login all returned HTTP 401 with body {\"error\":\"Invalid credentials\"}, with no 429/throttle responses and no 5xx crashes.",
      "[setup-credentials] First-run setup created user username=bright_test, email=bright@test.com, password=BrightTest123!. Prefer these credentials for auth.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=Authorization, prefix=\"\".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:3000/api/users/me. Do not mutate or re-encode it into a different identity. Evidence: unauth=403 ({\"message\":\"Forbidden resource\",\"error\":\"Forbidden\",\"statusCode\":403}), login=201, auth=200 ({\"firstName\":\"admin\",\"lastName\":\"admin\",\"id\":1,\"isAdmin\":true})"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "eZWAPbLg2SCTHo4Jt3CQSd",
      "updatedAt": "2026-07-08T13:47:45.612Z",
      "createdAt": "2026-07-08T13:47:45.612Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "Authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "{{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(.+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "hQg3feiBBdERvixzC7wwKH",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/me",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "uopzWeBYgQEC6ACqNwAAFt"
      },
      "staticHeaders": null
    }
  },
  "limits": {
    "scanPrepReplayCommands": [
      {
        "container": "brokencrystals-nodejs-1",
        "command": "printenv | sort | egrep 'DB|POSTGRES|DATABASE|NODE_ENV|URL|JWT|KEYCLOAK|GRPC|FASTIFY|DISABLE|CSRF|SESSION' || true"
      }
    ]
  },
  "hints": {
    "startup": [
      "Repo runs with `docker compose -f compose.local.yml up -d --build`; app is the `nodejs` service built from the repo Dockerfile and published on host port 3000, serving HTTP 200 on `/`. Node server listens on `0.0.0.0:3000`."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
