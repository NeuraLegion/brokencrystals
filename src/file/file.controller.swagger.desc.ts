export const SWAGGER_DESC_READ_FILE = `Reads the file from the provided path and the supplied content type and returns the file`;

export const SWAGGER_DESC_DELETE_FILE = `Deletes file at the given path`;

export const SWAGGER_DESC_SAVE_RAW_CONTENT = `Save raw content on server as a file`;

export const SWAGGER_DESC_READ_FILE_ON_SERVER = `Read file content on server as a file`;
