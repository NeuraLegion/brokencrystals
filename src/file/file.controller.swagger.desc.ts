export const SWAGGER_DESC_readFile = `
    Reads the file from the provided path and the supplied content type and returns the file
`;

export const SWAGGER_DESC_deleteFile = `
    Deletes file at the given path
`;

export const SWAGGER_DESC_saveRawContent = `
    Save raw content on server as a file
`;

export const SWAGGER_DESC_readFileOnServer = `
    Read file content on server as a file
`;
