export const SWAGGER_DESC_RENDER_REQUEST = `Template for rendering by doT. Expects plain text as request body`;

export const SWAGGER_DESC_REDIRECT_REQUEST = `Redirects the user to the provided url`;

export const SWAGGER_DESC_XML_METADATA = `Receives client's metadata in XML format. Returns the passed XML`;

export const SWAGGER_DESC_OPTIONS_REQUEST = `Returns the list of supported operations`;

export const SWAGGER_DESC_LAUNCH_COMMAND = `Launches system command on server`;

export const SWAGGER_DESC_CONFIG_SERVER = `Returns server configuration to the client`;
