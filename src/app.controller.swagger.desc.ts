export const SWAGGER_DESC_renderrequest = `
    Template for rendering by doT. Expects plain text as request body
`;

export const SWAGGER_DESC_redirectrequest = `
    Redirects the user to the provided url
`;

export const SWAGGER_DESC_xmlmetadata = `
    Receives client's metadata in XML format. Returns the passed XML
`;

export const SWAGGER_DESC_optionsrequest = `
    Returns the list of supported operations
`;

export const SWAGGER_DESC_launchcommand = `
    Launches system command on server
`;

export const SWAGGER_DESC_configserver = `
    Returns server configuration to the client
`;
