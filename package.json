{
  "name": "broken-crystals",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:client": "vite build && node scripts/copy-nginx-conf.mjs",
    "postbuild": "node scripts/copy-nginx-conf.mjs"
  }
}
